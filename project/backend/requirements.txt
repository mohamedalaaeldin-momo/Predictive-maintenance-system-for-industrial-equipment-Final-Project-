# IMPORTANT: scikit-learn is pinned to the exact version the pipeline was
# trained/pickled with (1.6.1). Loading a pickle with a different sklearn
# version silently breaks internals (confirmed while preparing this
# project: newer sklearn raised AttributeError on SimpleImputer/
# ColumnTransformer internals when unpickling a 1.6.1 model). Do not bump
# this version unless you re-train and re-pickle the model.
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
